# Project-Management-System
ProjectHub — a Vite + React (TypeScript) app for managing projects and tasks with Supabase auth. Features: sign-in, dashboard, projects list, project detail, create/manage tasks. Tailwind CSS + lucide icons. Run: npm install &amp;&amp; npm run dev
(https://crystal-arbitrary-first-pci.trycloudflare.com/login)
